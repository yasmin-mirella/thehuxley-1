#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, i, x, y;
    scanf("%d", &n);
    for(i=1;i<=n;i++){
        x=i*i;
        y=i*i*i;
         printf("%d %d %d\n", i, x, y);
    }
   
	return 0;
}
