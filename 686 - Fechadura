#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int r=0, m, n, i, k=0, v;
    
    scanf("%d%d", &n, &m);
    
    for(i=0; i<n; i++){
            scanf("%d", &v);//}
//   i=0;
      //  while(i<n-1){         
    
    r += abs(k);
    k = m - (v + k);
  //  if(r=m)
       
}
printf("%d\n", r);
	return 0;
}
