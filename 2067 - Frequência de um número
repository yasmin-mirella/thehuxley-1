#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x, y, a, b, i;
    b=0;
    scanf("%d%d", &x, &y);
    for(i=0;i<x;i++)
    {
        scanf("%d", &a);
        if(a==y){
            b++;
        }
    }
    printf("%d\n", b);
	return 0;
}
